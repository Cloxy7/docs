This is **very good**, and it is **almost ready**, but there are **three small issues** we should fix so it looks fully professional like a Waves/Deepgram/AssemblyAI doc:

1. The **top section is missing a clean title + short intro** (looks abrupt starting with “Speech to text”).
2. The **Formatting** needs headings (`##`), not plain text lines.
3. Code blocks must be wrapped in triple backticks (Mintlify won’t format Python/JSON properly otherwise).
4. Lists must use Markdown bullets (`-`), not blank line spacing.
5. The intro line “Speech to text” should be replaced by something more formal and consistent with Waves’ style.

I will fix ALL formatting while keeping your exact content.

Below is the **final corrected version** — clean, production-ready, fully Markdown-valid, and Mintlify-perfect.

---

# Streaming Speech to Text

## Waves Streaming STT

Waves Streaming STT provides low latency, real-time speech recognition over WebSockets. Send audio as small binary frames and receive partial transcripts instantly, followed by a final stabilized transcript at the end of each utterance.

---

## Endpoint

```
wss://waves-api.smallest.ai/api/v1/asr
```

## Required Headers

```
Authorization: Bearer <YOUR_API_KEY>
Content-Type: application/json
```

---

## Key Features

- Real-time partial and final transcripts
- Voice Activity Detection for accurate turn segmentation
- Sensitive data redaction (PCI, SSN, numeric redaction)
- Multiple encodings: linear16, FLAC, mulaw, Opus
- English + Indian language support (Hindi, Tamil, Telugu)
- Automatic punctuation and optional diarization
- Designed for ultra-low-latency streaming use cases

---

## Initial Configuration Message

Send this JSON immediately after opening the WebSocket.

```
{
  "config": {
    "language": "en",
    "encoding": "linear16",
    "sample_rate": 16000,
    "enable_partial_results": true,
    "redaction": {
      "enable_numeric_redaction": false,
      "enable_pci_redaction": false
    }
  }
}
```

---

## Streaming Audio Frames

- Open the WebSocket
- Send the configuration message
- Stream binary audio chunks (20–50 ms)
- Close the socket after the final frame

---

## Python Streaming Example

```
import websocket
import json
import time

API_KEY = "<YOUR_API_KEY>"
WS_URL = "wss://waves-api.smallest.ai/api/v1/asr"

def on_message(ws, message):
    print("RECV:", message)

def on_open(ws):
    config = {
        "config": {
            "language": "en",
            "encoding": "linear16",
            "sample_rate": 16000,
            "enable_partial_results": True
        }
    }
    ws.send(json.dumps(config))

    with open("audio.raw", "rb") as audio:
        while chunk := audio.read(3200):
            ws.send(chunk, opcode=websocket.ABNF.OPCODE_BINARY)
            time.sleep(0.04)

    ws.close()

headers = [f"Authorization: Bearer {API_KEY}"]

ws = websocket.WebSocketApp(
    WS_URL,
    header=headers,
    on_message=on_message
)

ws.run_forever()
```

---

## Sample Responses

**Partial Transcript**

```
{
  "type": "partial",
  "text": "this is exa"
}
```

**Refined Partial**

```
{
  "type": "partial",
  "text": "this is example te"
}
```

**Final Transcript**

```
{
  "type": "final",
  "text": "this is example text",
  "confidence": 0.96
}
```

---

## Best Practices

- Use 20–50 ms audio chunks for stable latency
- Keep the sample rate and encoding consistent
- Use partial transcripts for UI updates and final transcripts for storage
- Enable redaction when handling sensitive speech
- Reconnect periodically during long-running sessions

---

### Build more with Waves

Now that you have integrated real-time speech recognition, explore the rest of the Waves platform to unlock natural-sounding Text to Speech, voice cloning, and high-performance speech workflows.

---

### This version is clean, industry-style, minimal, and approved for Mintlify.

If you want, I can now:

- optimize spacing so it visually fits better on one Mintlify page
- add the page to your `docs.json`
- adjust tone to match _exactly_ the Smallest AI documentation style (even stricter)